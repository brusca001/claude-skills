# Model backend for video summarization
MOONSHOT_API_KEY=

# Telegram delivery — reuse the existing values from ~/.claude/skills/telegram/SKILL.md
# (bot @MrJoben_bot). Needed here too because the /schedule cloud routine can't reach
# the local telegram skill directly.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
